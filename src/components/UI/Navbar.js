export const Navbar = ({children}) => {
    return (
        <div>
            <div>
            I am the navbar
            </div>

            {/* This is where the content goes */}
            {children}
            
            </div>
    )
}